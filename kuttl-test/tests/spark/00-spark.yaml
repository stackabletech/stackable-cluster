apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: helm upgrade spark-operator stackable-dev/spark-operator --version="0.5.0-mr216" --install
  - command: pwd
  - command: ls -al
  - command: kubectl apply -f spark.yaml
  - command: kubectl get pods -o=name